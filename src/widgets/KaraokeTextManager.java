package widgets;

/**
 * Creates a manager for .txt files.
 * <ul>
 *     <li>Stores things like colour and size of a selected letter.</li>
 *     <li>Manages the selected letter and the file (next, previous, reset, length, remainingLenght), prevent spaces and /n from being selected)</li>
 * </ul>
 */
public class KaraokeTextManager {
}
